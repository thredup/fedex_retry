require 'spec_helper'

describe FedexTrackingClient do
  it 'has a version number' do
    expect(FedexTrackingClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
